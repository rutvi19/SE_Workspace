#include <stdio.h>
#include <conio.h>

int main() 
{
    int num,ans;
    
    printf("enter any number:");
    scanf("%d",&num);
    
    ans=5*num;
    printf("the required apples is %d",ans);
    return 0;
}
//accept the given number form user.i need to give 5 apples to each student.how many apples are required?