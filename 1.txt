1.SELECT * FROM product WHERE PRO_PRICE>=250;

2.SELECT PRO_NAME,PRO_PRICE FROM product ORDER BY PRO_NAME ASC,PRO_PRICE DESC;

SELECT PRO_PRICE,PRO_NAME FROM product ORDER BY PRO_PRICE DESC,PRO_NAME ASC;

3.SELECT PRO_NAME, PRO_PRICE FROM product WHERE PRO_PRICE = (SELECT MIN(PRO_PRICE) FROM product);

4.SELECT PRO_PRICE, AVG(PRO_PRICE) AS AVG_PRICE FROM product GROUP BY PRO_PRICE;

5.SELECT AVG(PRO_PRICE) AS average_total FROM product;